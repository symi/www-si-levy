{
  "scripts": {
    "clean": "del .vuepress/dist",
    "dev": "vuepress dev",
    "build": "npm run clean && vuepress build",
    "deploy": "npm run build && firebase deploy --only=hosting"
  },
  "devDependencies": {
    "del-cli": "^1.1.0",
    "firebase": "^5.5.2",
    "vuepress": "^0.14.4"
  },
  "dependencies": {
    "date-fns": "^1.29.0"
  }
}
